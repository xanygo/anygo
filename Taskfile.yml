# https://taskfile.dev

version: '3'

vars:
  GREETING: Hello, World!

env:
  FORCE_COLOR: yes

tasks:
  default:
    cmds:
      - echo "hello"
  test:
    cmds:
      - gorgeous
      - go vet ./...
      - staticcheck ./...
      - go test -race -count=1 ./...
      - cd cmd/example/db && go test -race -count=1 ./...
  testdb:
    cmds:
      - gorgeous
      - go vet ./...
      - staticcheck ./...
      - cd cmd/example/db && go test -race -count=1 ./...
  trs:
    env:
      anygo-ut-redis: redis://127.0.0.1:6379
    cmds:
      - go test -race  github.com/xanygo/anygo/xredis
  trc:
    cmds:
      - go test -race  github.com/xanygo/anygo/xredis
